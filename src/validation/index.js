const validationRepeatedArguments = require('./validation-repeated-arguments'),
      validationConfig = require('./validation-config'),
      validationInput = require('./validation-input'),
      validationOutput = require('./validation-output')

module.exports = {
  validationRepeatedArguments,
  validationConfig,
  validationInput,
  validationOutput
};